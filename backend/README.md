# backend

## Local development

Start local backend

```shell
yarn nx run backend:start:local
```

Stop local hasura backend

```shell
yarn nx run backend:stop:local
```

Destroy local hasura backend

```shell
yarn nx run backend:destroy:local
```
